# GCP-2 — Structure and identity

Includes GCP-1. A conforming implementation produces stable UIDs, typed structured objects, schema versions, canonical representations, metadata, and locators. Filenames and paths are not identity.
